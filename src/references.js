const WEBAPP_PATH = './remote';

const SCRIPTS = {
  tools: `${WEBAPP_PATH}/tools.js`,
  extensionsManager: `${WEBAPP_PATH}/extensions-manager.js`,
  dependencies: `${WEBAPP_PATH}/dependencies.js`,
};

export { SCRIPTS };
export default { SCRIPTS };
