const WEBAPP_PATH = 'remote';

const EXTENSIONS_FOLDER = 'extensions';

const SCRIPTS = {
  setup: `${WEBAPP_PATH}/setup.js`,
  scrappers: `${WEBAPP_PATH}/scrappers.js`,
  messenger: `${WEBAPP_PATH}/messenger.js`,
};

export { SCRIPTS, EXTENSIONS_FOLDER };
